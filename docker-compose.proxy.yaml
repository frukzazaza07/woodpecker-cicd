services:
  nginx:
    image: nginx:stable-alpine
    restart: unless-stopped
    ports:
      - "888:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - proxy

networks:
  proxy:
    driver: bridge
    name: "${NGINX_NETWORK}"